import LoginLogoutButton from "./LoginLogoutButton";

const Home = () => {
  return (
    <div className="root">
      <h1>Log in with OAuth2</h1>
      <LoginLogoutButton />
    </div>
  );
};

export default Home;
